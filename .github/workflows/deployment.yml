# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_deployment --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: deployment

on:
  workflow_dispatch:
    inputs:
      BUMP_VERSION:
        description: 'Bump Version (major|minor|patch|alpha|beta):'
        required: true
      BUMP_MESSAGE:
        description: 'Bump Message (optional):'
        required: false

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run './build.cmd BumpVersion'
        run: |
          export GOPATH=/home/runner/go
          echo $GOPATH
          go get -v github.com/kyoh86/git-vertag
          ls -halF $GOPATH
          ls -halF $GOPATH/bin
          ls -halF $GOPATH/src/github.com/*
          ./build.cmd BumpVersion
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
            BUMP_VERSION: ${{ github.event.inputs.BUMP_VERSION }}
            BUMP_MESSAGE: ${{ github.event.inputs.BUMP_MESSAGE }}
      - uses: actions/upload-artifact@v1
        with:
          name: packages
          path: output/packages
