# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_deployment --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: deployment

on:
  workflow_dispatch:
    inputs:
      BUMP_SEMANTIC:
        description: 'Bump Semantic (major|minor|patch):'
        required: true
      BUMP_STABILITY:
        description: 'Bump Stability (alpha|beta|release):'
        required: true
        default: alpha

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run './build.cmd BumpVersion'
        run: |
          export GOPATH=/home/runner/go
          # echo $GOPATH
          # go get -v github.com/kyoh86/git-vertag
          # ls -halF $GOPATH
          # ls -halF $GOPATH/bin
          # ls -halF $GOPATH/src/github.com/*
          # git remote -v
          ./build.cmd BumpVersion
          ./build.cmd Publish
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
            BUMP_SEMANTIC: ${{ github.event.inputs.BUMP_SEMANTIC }}
            BUMP_STABILITY: ${{ github.event.inputs.BUMP_STABILITY }}
      - uses: actions/upload-artifact@v1
        with:
          name: packages
          path: output/packages
